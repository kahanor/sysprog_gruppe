README

Zu Uebung 1, Aufgabe 3:
Wir haben die folgenden Optionen verwendet, um unseren Code zu formatieren.
Die Optionen befinden sich ebenfalls im .indent.pro File. Befindet sich diese
Datei in dem Verzeichnis, in dem GNU indent aufgerufen wird, oder im Home
Verzeichnis, dann werden die Optionen automatisch benutzt und man muss sie nicht
bei jedem Aufruf neu eingeben.

-nbad -bap -bbb -lc100 -cdb -sc -nsob -br -nce -ncdw -cli0 -npcs -nss -cs -saf
-saw -sai -nprs -di1 -nbc -npsl -brs -brf -i2 -ci2 -lp -ip0 -bbo -hnl -sob
